policies:
  - name: s3-tag-missing
    description: |
      Report S3 buckets missing tags
    resource: s3
    region: us-east-1
    filters:
    filters:
      - or:
          - "tag:Owner": absent
          - "tag:CostCenter": absent
          - "tag:Project": absent
 